<%= nested_form_for(@worksite) do |f| %>
  <% if @worksite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@worksite.errors.count, "error") %> prohibited this worksite from being saved:</h2>

      <ul>
      <% @worksite.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :reason %><br />
    <%= f.text_field :reason %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :work_start_on %><br />
    <%= f.text_field :work_start_on, :id => "work_start" %>
  </div>
  <div class="field">
    <%= f.label :work_end_on %><br />
    <%= f.text_field :work_end_on, :id => "work_end" %>
  </div>

  <%= f.fields_for :items %>
  <p><%= f.link_to_add t('.add_an_account'), :items %></p>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
