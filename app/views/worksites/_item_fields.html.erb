<table>
	<tr>
		<td>
			<%# f.select :material_id, Material.all.collect {|m| [m.name, m.id] } %>
			<%= f.label :name %><br />
			<%= f.text_field :name, :class => "material_label" %>
			<%= f.hidden_field :material_id, :class => "material_value" %>
			<%= f.hidden_field :worksite_id %>
		</td>
		<td class="field">
   			<%= f.label :quantity %><br />
   			<%= f.text_field :quantity %>
 		</td>
 		<td class="field">	
   			<%= f.label :totalTF %><br />
   			<%= f.text_field :totalTF %>
 		</td>
		<td class="field">	
   			<%= f.label :tva %><br />
   			<%= f.text_field :tva %>
		</td>
		<td><%= f.link_to_remove t('destroy')  %></td>
	</tr>
</table>